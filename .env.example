# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/linkedin_planner"

# NextAuth
NEXTAUTH_SECRET="your-secret-here-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Auth (credentials mode - bruges til demo login)
# Ingen yderligere env vars kræves for credentials provider

# Uploads (valgfrit - til produktion)
# UPLOAD_DIR="./public/uploads"
